"use strict";

Object.defineProperty(exports, "__esModule", { value: true });
var index_1 = require("./index");
var parser = require("../../utils/fileParser/index");
var path = require("path");
var util = require("util");
var charts = require("../../utils/charts/index");
var dataSet = parser.parseFile(path.join(__dirname, './dt.txt'));
var labels = ['age', 'prescript', 'astigmatic', 'tearRate'];
var dt = new index_1.default(dataSet, labels);
var result = dt.classify(labels, ["young", "myope", "no", "reduced"]);
console.log(util.inspect(dt.getTree(), { depth: null }));
charts.drawDT(dt.getTree(), {
    width: 600,
    height: 400
});
//# sourceMappingURL=../../maps/algorithm/DT/test.js.map
